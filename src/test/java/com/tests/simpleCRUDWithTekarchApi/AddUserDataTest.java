package com.tests.simpleCRUDWithTekarchApi;

import org.testng.AssertJUnit;//crud 

import com.test.helpers.ReusableMethods;
import com.test.helpers.UserServiceHelper;

import io.restassured.http.Header;
import io.restassured.response.Response;

public class AddUserDataTest extends UserServiceHelper {
	public static void addUserDetails() {
		/*
		 * //Response res=addUserData(); ReusableMethods.verifyStatusCodeis(res, 200);
		 * String status=ReusableMethods.getJsonPathData(res,"status");
		 * AssertJUnit.assertEquals(status, "sucess");
		 */
		
	}

}
