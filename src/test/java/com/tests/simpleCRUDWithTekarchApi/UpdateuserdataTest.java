package com.tests.simpleCRUDWithTekarchApi;

import org.testng.AssertJUnit;

import com.test.helpers.ReusableMethods;
import com.test.helpers.UserServiceHelper;

import io.restassured.response.Response;

public class UpdateuserdataTest extends UserServiceHelper {
	public static void updateUserDetails() {
		/*
		 * Response response=updateUserData();
		 * ReusableMethods.verifyStatusCodeis(response, 200);
		 * System.out.println("data has been updated ==="); String status=
		 * ReusableMethods.getJsonPathData(response,"status");
		 * AssertJUnit.assertEquals(status, "sucess");
		 */
	}

}
